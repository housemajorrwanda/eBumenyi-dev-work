[phases.setup]
nixPkgs = ["python311"]
aptPkgs = [
  "git",
  "libgomp1",
  "libglib2.0-0",
  "libsm6",
  "libxext6",
  "libxrender1",
  "libsndfile1",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "./start-docker.sh"
